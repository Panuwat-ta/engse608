<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet: สำหรับส่งข้อมูลไปยัง Google Apps Script และ Google Sheets API -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- Network State: ตรวจสอบสถานะการเชื่อมต่ออินเทอร์เน็ต -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- WiFi State: ตรวจสอบสถานะ WiFi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Location: สำหรับดึงพิกัด GPS ตอนสมัครสมาชิก -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Camera: สำหรับถ่ายรูปเครื่องมือ (ถ้ามีฟีเจอร์นี้) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <!-- Storage: สำหรับบันทึกรูปภาพและไฟล์ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    
    <!-- สำหรับ Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    
    <!-- Notification: สำหรับแจ้งเตือนการยืม-คืน -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    
    <!-- Vibrate: สำหรับการสั่นเมื่อมีการแจ้งเตือน -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    
    <!-- Wake Lock: เพื่อให้แอปทำงานในพื้นหลังสำหรับ sync ข้อมูล -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Features ที่ไม่จำเป็น (optional) -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>

    <application
        android:label="Community Tools"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- Google Play Services (สำหรับ Location และ Auth) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
            
    </application>
    
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- สำหรับเปิดเบราว์เซอร์ -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- สำหรับแชร์ข้อมูล -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="*/*"/>
        </intent>
    </queries>
</manifest>
